apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth-service-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: auth-service
  endpoints:
    - port: "8001"
      path: /metrics
      interval: 15s